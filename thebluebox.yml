---
- hosts: all
  remote_user: pi
  gather_facts: yes
  become: yes

  roles:
    - { role: ssh, tags: ['ssh'] }
    - { role: sslh, tags: ['sslh'] }
    - { role: owncloud, tags: ['owncloud'] }
    - { role: s3ql, tags: ['s3l'] }
    - { role: attic, tags: ['attic'] }
    - { role: duckdns, tags: ['duckdns'] }

  # todo: attic crontab
  # todo: https
